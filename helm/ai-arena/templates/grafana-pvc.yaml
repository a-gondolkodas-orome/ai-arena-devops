{{- if not .Values.localDeployment }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana
  namespace: default
  finalizers:
    - kubernetes.io/pvc-protection
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  selector:
    matchLabels:
      component: grafana
{{- end }}